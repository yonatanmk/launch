To get started run the following:

```sh
$ npm install
$ webpack-dev-server
```

To run the back-end server, run the following in a new terminal tab:

```sh
$ cd back_end
$ bundle install
$ ruby server.rb
```
